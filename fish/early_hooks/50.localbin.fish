# if [ -d "$HOME/.local/bin" ]
#   if not contains -- "$HOME/.local/bin" $fish_user_paths
#     set -U fish_user_paths "$HOME/.local/bin" $fish_user_paths
#   end
#   Z_VERBOSE_LOG DONE 'initialize ~/.local/bin'
# end
